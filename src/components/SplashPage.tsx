import React from "react";
import { ConnectedProps, connect } from "react-redux";

const SplashPage : React.FC = (props) => {
  return (
    <div>
      TODO:// Write the splash page
    </div>
  );
}

export default SplashPage;